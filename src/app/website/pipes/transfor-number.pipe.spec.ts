import { TransforNumberPipe } from './transfor-number.pipe';

describe('TransforNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new TransforNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
